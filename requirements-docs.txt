# Pinned docs toolchain — consumed by `.github/workflows/docs.yml` and
# reproducible locally via `pip install -r requirements-docs.txt`.
#
# Upgrading these versions is a docs-only change; bump, rebuild, and
# visually check one rendered page before merging. No CERT-ATTEST gate
# applies here — MkDocs output is not a cert artefact.

mkdocs==1.6.1
mkdocs-material==9.5.49
mkdocs-same-dir==0.1.3
pymdown-extensions==10.12
# Pygments 2.19 dropped tolerance for filename=None, which pymdownx.highlight
# 10.12 passes through. Pin to 2.18.x until pymdown-extensions fixes the
# default. Tracking: facelessuser/pymdown-extensions issue 2502-family.
pygments==2.18.0
