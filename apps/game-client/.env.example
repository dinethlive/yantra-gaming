# Base URL for the RGS HTTP API (operator-facing REST).
VITE_RGS_BASE_URL=http://localhost:4500

# Base URL for the Socket.IO endpoint the iframe connects to.
VITE_RGS_SOCKET_URL=http://localhost:4500
